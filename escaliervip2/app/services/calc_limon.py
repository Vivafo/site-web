# Ici tu wraps tes fonctions de ton app Escalier VIP
def calc_limon(rise: float, run: float, steps: int):
    # TODO: appeler ton module existant et renvoyer un petit dict propre
    longueur = steps * (rise + run)  # placeholder
    return {"longueur": longueur, "steps": steps}
